// FR-ATTR — client-facing copy for The Standard, single-sourced so the leave-behind
// (exportHtml.ts) can never drift from the screen (FrontDoorMapAct.tsx).
//
// Plain-English law (operator ruling 2026-07-23): client-facing surfaces name no
// framework (Jobs-to-be-Done, ODI) and print no internal tag (the taxonomy_version
// / fd1-* id). Method-attribution belongs in internal provenance, never on the
// client's screen or their leave-behind.
//
// PENDING OPERATOR SIGNATURE (house pattern) — both strings are drafts until signed.

// The attribution printed under every rendered standard map (screen + export).
export const STANDARD_ATTRIBUTION_LINE = "Standard reference model for your industry";

// The act's intro sentence (screen only — the export opens the standard section
// with the map itself, not this line).
export const STANDARD_INTRO_SUB =
  "The standard way your industry's customer gets this job done — the same for everyone. Not a reading of your business.";
