// Name-the-moves METHOD recaps — OPERATOR-SIGNED 2026-08-03. Rendered verbatim via
// ActRecap. Single-sourced so the two GAP mount points (DiagnoseMarketAct on /client-view,
// GapAct on the rail) can never drift. METHOD only — each names a move, identical for every
// client; no SITUATION result summary lives here (that is generated, never hand-authored).
//
// The OUTSIDE-READ recap is intentionally absent: its act's hasContent gate is ambiguous
// (the move spans hero/findings/market/positioning and MovementShell always renders its
// divider), so it is blocked on an operator ruling — not shipped in this gate.

export const CHECK_RECAP =
  "That was the check — you weighed our read against what you know. Where you pushed back is where the gap lives.";

export const GAP_RECAP =
  "That was the gap — what you say, set beside what the outside sees. Neither is wrong; the choice is which one your strategy runs on.";

export const CHOOSE_RECAP =
  "That was the choose — the direction we'd take together, drawn from your read and ours.";
