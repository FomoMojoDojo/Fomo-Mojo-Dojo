# M1 + M1.1 Verification Diagnostic
**Date:** 2026-05-20  
**Branch:** strategic-object-graph  
**Scope:** Read-only audit of 5 skipped-inspection items from M1 and M1.1

---

## Overall Verdict

**4 PASS · 0 GAP · 1 NOT TESTED · 1 MINOR OMISSION**

All code-level checks pass. No blockers for A79a. One unlisted deferral field
(`assumptions_json`) should be added to M1.5 scope. Cafe Barra live-DB
verification is NOT TESTED — UI walkthrough required after deployment.

---

## Phase 1 — A35 Preservation Pattern

**Verdict: PASS**

All three regeneration paths use the `LIKE 'manual_%'` pattern, which
implicitly covers both `manual_inline` and `manual_a5_recovery`:

| File | Lines | Pattern |
|------|-------|---------|
| `supabase/functions/research-company/index.ts` | 6458 | `.not("source", "like", "manual_%")` |
| `supabase/functions/refresh-cascade/index.ts` | 141, 244 | `.like(...)` guard + `.not(...)` delete filter |
| `supabase/functions/refresh-positioning/index.ts` | 142, 245 | same pattern |

Each function returns `status: 'skipped_manual_preserved'` without invoking
the LLM when the existing row matches `LIKE 'manual_%'`, and the delete/replace
paths use `.not("source", "like", "manual_%")` to exclude all manual rows.

**Evidence from live DB:** `routes` for Cafe Barra (company
`58b2b15b-bada-4bcd-9c12-b7e66a37d0bc`) contains 9 rows with
`source='manual_a5_recovery'` (updated today at 07:25), confirming the
pattern protects a live `manual_` prefix across a full B-migration run.

No exact-match list found — pattern is LIKE-based throughout. ✓

---

## Phase 2 — Leg Alignment Re-eval Coverage

**Verdict: PASS**

### Caller layer (`ClientRefinePreviewRoutesView.tsx:2593–2597`)

```typescript
async function handleSaveRouteField(routeOrLegId, field, value) {
  if (!activeCompany?.id) return;
  await saveManualEdit("route", routeOrLegId, activeCompany.id, field, value);
  supabase.functions.invoke("evaluate-route-alignment", {
    body: { route_id: routeOrLegId, company_id: activeCompany.id }
  }).catch(() => {});
  setRoutesRefreshKey((k) => k + 1);
}
```

The `routeOrLegId` is passed unconditionally — no level check. Both
`level='route'` rows and `level='leg'` rows flow through the same path.

### Edge function layer (`supabase/functions/evaluate-route-alignment/index.ts:65–70`)

```typescript
const { data: routeData } = await db
  .from("routes")
  .select("id, title, short_description, category, ...")
  .eq("id", route_id)
  .eq("company_id", company_id)
  .maybeSingle();
```

Fetches the row by `id` only — no `level` filter. A leg row is retrieved
and evaluated identically to a route row. ✓

### Sibling save handler audit

Grep of `src/views/client/workshop/tabs/` for `onSaveField`, `handleSaveRouteField`,
`saveManualEdit` returned no results. The only inline save path for route/leg
fields is `handleSaveRouteField` in `ClientRefinePreviewRoutesView.tsx`.

---

## Phase 3 — useOdiNeeds refreshKey Integration

**Verdict: PASS**

### Hook layer (`src/hooks/useOdiNeeds.ts`)

- **Signature** (line 50): `export function useOdiNeeds(companyId?: string, refreshKey = 0)`
- **Deps array** (line 127): `}, [companyId, refreshKey]);`

Both conditions met — hook re-fetches whenever `refreshKey` increments.

### Call-site layer (`ClientRefinePreviewWorkshopView.tsx`)

- **Hook instantiation** (line ~514): `useOdiNeeds(companyId, needsRefreshKey)` ✓
- **`setNeedsRefreshKey` call sites:**
  - `handleSaveNeedField` (line ~731) — fires on every `odi_canonical_statement` save
  - `handleCascadeNarrativeInlineEdit` (added M1.1 Phase 4a) — fires after cascade saves
  - `handleAcceptCascadeProposal` (line ~693) — fires after cascade proposal accept
  - `handleAcceptOpportunityProposal` (line ~1125/1151) — fires after opportunity proposal accept
  - `NeedsInputBox.onAdded` (line ~1751) — fires on social need additions

The UI reflects saves on next render cycle. The M1 memory caveat
("value won't reflect until next reload") is resolved. ✓

---

## Phase 4 — Array-Shape Audit

### `strategy_cascades`

| Column | DB type | Runtime shape | Classification | Status |
|--------|---------|---------------|----------------|--------|
| `capabilities_json` | `jsonb DEFAULT '[]'` | `CascadeItem[]` `{name, status, note?, evidence?, unverified?}` | array-of-objects | Correctly deferred → M1.5 |
| `management_systems_json` | `jsonb DEFAULT '[]'` | `CascadeItem[]` | array-of-objects | Correctly deferred → M1.5 |
| `assumptions_json` | `jsonb DEFAULT '[]'` | `CascadeAssumption[]` `{assumption, tested, outcome?, note?}` | array-of-objects | **⚠ Rendered in StrategyOrgPanel but NOT in M1.5 deferral list** |

**`assumptions_json` omission:** `StrategyOrgPanel.tsx:36` renders it under
the label "Assumptions" using the same list editor as capabilities and
management_systems. It was not included in the M1 deferral list. Recommend
adding to M1.5 scope alongside the other two cascade array fields.

### `positioning_canvases`

| Column | DB type | Runtime shape | Classification | Status |
|--------|---------|---------------|----------------|--------|
| `competitive_alternatives_json` | `jsonb DEFAULT '[]'` | `PositioningItem[]` `{id, name, description, highlighted?}` | array-of-objects | Correctly deferred → M1.5 |
| `unique_attributes_json` | `jsonb DEFAULT '[]'` | `PositioningItem[]` | array-of-objects | Correctly deferred → M1.5 |

No `competitors` or `differentiators` columns exist as separate columns —
these map to `competitive_alternatives_json` and `unique_attributes_json`.

### `routes`

| Column | DB type | Documented shape | Classification | Status |
|--------|---------|------------------|----------------|--------|
| `rejected_alternatives` | `jsonb DEFAULT '[]'` | `[{alternative_title, rejection_reason, considered_at}]` | array-of-objects | Correctly deferred → M1.5 |
| `what_would_have_to_be_true` | `jsonb DEFAULT '[]'` | `[{condition, satisfied_flag, evidence_refs}]` | array-of-objects | Correctly deferred → M1.5 |

Source: `migrations/20260603200000_a5_route_leg_action_hierarchy.sql:16–17`.

### `odi_needs`

| Column | DB type | Classification | Status |
|--------|---------|----------------|--------|
| `odi_canonical_statement` | `TEXT NULL` | text (single string) | **In-scope M1.1 — wired in Phase 3** ✓ |

Source: `migrations/20260518000001_add_odi_canonical_statement.sql:5`.

**Summary:** All 7 fields in the original M1.5 deferral list are genuinely
array-of-objects — no miscategorization. One additional field (`assumptions_json`)
should be added to M1.5. No fields were incorrectly deferred.

---

## Phase 5 — Cafe Barra Verification Evidence

**Verdict: NOT TESTED**

Live-DB query of Cafe Barra (`58b2b15b-bada-4bcd-9c12-b7e66a37d0bc`) found
no rows with `source='manual_inline'` in any surface:

| Table | Manual rows found | Source value(s) |
|-------|-------------------|-----------------|
| `strategy_cascades` | 1 | `manual_60ef1fe7-...` (UUID-style, pre-M1) |
| `positioning_canvases` | 1 | `manual_26c7af7e-...` (UUID-style, pre-M1) |
| `routes` | 10 | `manual_a5_recovery` (×9), `manual_60f51302-...` (×1) |
| `odi_needs` | 1 | `manual_755ab389-...` (UUID-style, pre-M1) |

The M1.1 inline-edit code paths have not been exercised in the live DB.
No `evidence_baseline_captured_at` advancement from inline edits to compare.

**Note on routes:** The 9 `manual_a5_recovery` rows (all `updated_at` 2026-05-20T07:25:17) 
were written by today's B-migration route recovery run. Their presence confirms
the A35 `manual_%` LIKE pattern is actively protecting live data.

**Action required:** Manual UI walkthrough on Cafe Barra before A79a:
1. Open Workshop → Strategy tab → edit one narrative field inline → confirm save indicator + no LLM re-gen triggered
2. Open Workshop → Needs tab → edit one `odi_canonical_statement` → confirm save + re-eval fires
3. Open Routes → edit a leg title → confirm `evaluate-route-alignment` fires (visible in Supabase edge function logs)
4. Verify `source='manual_inline'` appears in at least one surface via DB query after walkthrough

---

## Recommendation

**Proceed to A79a** — no code-level blockers.

**Before A79a ships:** complete the Cafe Barra UI walkthrough (Phase 5 above)
to generate live `manual_inline` evidence and confirm end-to-end save flow.

**Add to M1.5 scope:** `assumptions_json` (strategy_cascades) — array-of-objects
editor identical to capabilities/management_systems, currently unlisted.
See `StrategyOrgPanel.tsx:36` and `src/lib/types.ts:153–158` for shape.
